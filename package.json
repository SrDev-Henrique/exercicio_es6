{
  "name": "exercicio-es6",
  "version": "1.0.0",
  "description": "ES6 exercise - filter approved students",
  "main": "dist/index.js",
  "scripts": {
    "build": "babel src -d dist",
    "start": "npm run build && node dist/index.js"
  },
  "devDependencies": {
    "@babel/cli": "^7.26.4",
    "@babel/core": "^7.26.0",
    "@babel/preset-env": "^7.26.0"
  }
}
